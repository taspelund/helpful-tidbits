[package]
name = "trip"
version = "0.1.0"
edition = "2021"

[dependencies]
byteorder = "1.5.0"
libc = "0.2.169"
serde = { version = "1.0.217", features = ["derive"] }
socket2 = { version = "0.5.7", features = ["all"] }
